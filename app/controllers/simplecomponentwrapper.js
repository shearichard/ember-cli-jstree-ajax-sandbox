import Controller from '@ember/controller';

export default Controller.extend({
  firstTitle:"First from component 0 (Wrapper)",
  secondTitle:"Second from component (Wrapper)",
  firstNumber:2001,
  secondNumber:2011
});
